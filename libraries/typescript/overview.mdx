---
title: "Overview"
description: "The Typescript Client for the Keyforge API"
---

## Install
<Tabs>
    <Tab title="npm">
        ```bash
        npm install @keyforgecloud/lib
        ```
    </Tab>
    <Tab title="pnpm">
        ```bash
        pnpm add @keyforgecloud/lib
        ```
    </Tab>
    <Tab title="yarn">
        ```bash
        yarn add @keyforgecloud/lib
        ```
    </Tab>
    <Tab title="bun">
        ```bash
        bun install @keyforgecloud/lib
        ```
    </Tab>
</Tabs>
